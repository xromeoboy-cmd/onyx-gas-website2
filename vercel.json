{
  "version": 2,
  "builds": [
    {
      "src": "api/**/*.js",
      "use": "@vercel/node"
    },
    {
      "src": "public/**",
      "use": "@vercel/static"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/public/$1"
    }
  ]
}
```

4. Commit changes

#### **Add File 3: .gitignore**

1. **"Add file" → "Create new file"**
2. Name: `.gitignore`
3. Paste:
```
node_modules/
.env
.env.local
.vercel
*.log
.DS_Store
